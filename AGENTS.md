# AGENTS.md - 开发指令、v4 架构约束与版权规范

## 0. Constellation 1.0 当前交付优先级（高于后续版本功能清单）

Constellation 当前实施版本是 **1.0 最小正式可用版**。除本节明确启用的能力外，本文中知识树、JSON Canvas、图谱、仪表盘、语义检索、同步、备份、迁移、Git、多工作区、插件、AI Agent 等内容均为 2.0、3.0 或 Lab 路线图，不得在 1.0 运行时展示入口、占位按钮或伪造实现。

### 0.1 1.0 唯一验收闭环

```text
独立快捷捕获 -> 真实 Markdown 落盘 -> 当前文件编辑 -> 本地搜索/引用
-> OpenAI-compatible 整理建议 -> 预览与确认 -> 受控写入 -> 原始碎片保留
```

1.0 只启用：`workspace`、`documents`、`capture`、`editor`、`search`、`references`（Wiki-Link 与相对 Markdown 链接）、`suggestions`、OpenAI-compatible `ai` 配置和基础 `settings`。AI 未配置或不可用时，捕获、编辑、搜索与引用必须继续可用。

### 0.2 1.0 模块与依赖边界

前端依赖方向固定为：`app shell -> 领域 UI -> src/core-client -> Typed Tauri IPC -> Rust service -> 文件系统`。

- 领域 UI 不得直接调用 `invoke`、读取文件系统或跨领域读取 Store。
- `src/core-client/` 是唯一 IPC 入口；所有失败统一为 `{ code, message, details, retryable }`。
- `workspace` 负责应用内路径连接、最近工作区和工作区根约束；不得调用系统原生文件选择器。
- `documents` 负责 UUID v7、frontmatter 保留、revision、原子写入、移动、回收站和恢复。
- `capture` 与 `editor` 只能共享文档身份和 revision，不能维护第二份正文状态。
- `search`、`references` 和 `suggestions` 必须由 Rust 查询或写入；前端不得扫描全库正文或直接写入目标文件。

### 0.3 1.0 硬切换

`notes_*`、`categories_*` 和 `metadata.json` 不是 1.0 的兼容层。替代链路完成后，必须在同一变更中清理其运行时调用、IPC 命令、Store、类型、测试和 README 描述；不得长期保留双轨事实源。

### 0.4 1.0 UI 红线

常驻导航只显示编辑器、搜索和设置。快捷捕获通过独立便签窗口、全局快捷键和托盘入口提供；主界面只展示其活动草稿摘要。编辑器为单一当前文件的最小可视化 Markdown 编辑器，源码仅在“高级”菜单暂时打开。关系栏默认折叠，只显示当前文件的引用与反向链接。

### 0.5 版本分支与成熟合并策略

版本分支用于隔离尚未成熟的版本能力，不允许用分支长期保存无法验证的半成品。稳定主线与版本开发线固定如下：

```text
main                 已发布、可用且经过完整验收的版本
release/1.0          1.0 稳定、补丁与发布准备
develop/2.0          从 v1.0.0 演进的 2.0 能力
develop/3.0          基于成熟 2.0 契约演进的 3.0 能力
```

- `main` 只接受成熟版本的合并；每次正式发布必须创建不可变标签，如 `v1.0.0`、`v2.0.0`、`v3.0.0`。
- 1.0 缺陷修复进入 `release/1.0`；通过验证后合并到 `main`，不得夹带 2.0/3.0 功能。
- 2.0 从 `v1.0.0` 或已合并的 1.0 稳定主线创建；其短生命周期功能分支命名为 `feature/2.0/<scope>` 并合入 `develop/2.0`。
- 3.0 若依赖 2.0 的数据模型或 Core contract，必须从 `develop/2.0` 创建；只有完全独立、且不修改既有事实源/契约的能力才可从稳定标签并行开发。
- 稳定分支中的重要修复必须定期前向合并到 `develop/2.0` 与 `develop/3.0`；优先使用可审查的合并提交，避免长期漂移后一次性大合并。紧急情况下才使用精确、可追溯的 cherry-pick。
- 版本分支只有在功能闭环、迁移/兼容策略、测试与打包验收完成后，才能通过 PR 合并到 `main`；不因“计划已写完”或“代码已存在”而提前合并。
- 版本分支不改变 1.0 的运行时边界：2.0/3.0 源码、原型与计划可保留或归档，但不得在 1.0 导航、IPC、服务注册或发布产物中暴露。

本文件用于指导 AI 编程助手在 Constellation 项目中的行为。每次开始代码生成、文件修改、架构设计或发布工作前，Agent 必须重新阅读本文件并严格遵守。

## 1. 项目基本信息

- **原始项目**：floral-notepaper（MIT 许可证），版权归 Achilng 所有。
- **二次开发项目**：Constellation（Maplar 修改版），修改部分版权归 Maplar 所有。
- **整体许可证**：MIT。必须保留原始版权声明，不得删除、修改或重命名 `LICENSE`。
- **产品定位**：面向个人知识工作者的碎片化知识记录软件与 AI 知识数据库。
- **产品哲学**：花笺的优雅 × FlClash 的秩序 × Obsidian 的智慧。
- **事实源**：用户工作区中的 Markdown、知识树 Markdown（`.mindmap.md`）、标准 JSON Canvas、附件和真实文件夹；索引和 AI 数据均为可重建派生数据。

Constellation v4 的核心闭环是：快速记录碎片、用真实文件管理知识、通过引用与图谱建立联系、用知识树梳理复杂学科架构、用 AI 检索和辅助整理、通过自由卡片仪表盘回顾知识。

## 2. 版权合规强制性规则（最高优先级）

Agent 在代码生成、文件修改和构建打包时必须遵守：

1. 不得删除、修改或重命名 `LICENSE` 文件。
2. 生成任何**新代码文件**（`.ts`、`.tsx`、`.rs`、`.js`、`.css`、`.html` 等）时，必须在文件头部添加：

   ```typescript
   /**
    * @copyright Copyright (c) 2026 Maplar
    * 基于 floral-notepaper 二次开发新增
    */
   ```

3. 修改现有代码文件时，不得移除或覆盖已有版权声明；若无声明则补充：

   ```typescript
   /**
    * @copyright 原始代码版权归 Achilng 所有 (Copyright (c) 2026 Achilng)
    * 基于 MIT 许可证授权
    *
    * 修改部分版权：Copyright (c) 2026 Maplar
    * 修改说明：[简述修改内容]
    */
   ```

4. `README.md` 等项目文档必须保留致谢与版权章节。
5. 构建产物必须包含 `LICENSE`，并保留 Achilng 与 Maplar 的版权声明。
6. 禁止建议用户移除版权声明，或在不保留 MIT 声明的情况下转为闭源。
7. 每次对话开始时，第一条回复必须输出：

   `[版权提醒] 本项目中 floral-notepaper（MIT）版权归 Achilng 所有，二次修改部分版权归 Maplar 所有。请始终保留 LICENSE 及源码中的版权声明。`

8. 根目录 `LICENSE` 必须保留 floral-notepaper 原始 MIT 文本，不得用 Maplar 声明覆盖。Maplar 对新增或修改部分的声明应放在源码头、README、构建关于页或后续独立的 `NOTICE`/`COPYRIGHT` 文件中。
9. Obsidian、FlClash、OpenAI、Anthropic 等名称仅可用于兼容性说明、供应商选择或设计来源说明；不得复制其专有代码、图标、Logo、截图、帮助文案、品牌配色组合或像素级界面，也不得暗示官方授权、隶属或合作。FlClash 源码采用 GPL-3.0，本项目只参考模块化卡片秩序，不得复制、改写或移植其源码和资产。
10. JSON Canvas 仅依据公开的 JSON Canvas 1.0 规范实现。若未来直接复制 JSON Canvas 官方仓库中的代码或资源，必须同时履行其 MIT 许可证的版权和许可文本保留义务。
11. 发布前必须核对 `package.json`、`Cargo.toml` 和打包配置中的许可证元数据，生成第三方依赖许可证清单；发现 GPL、AGPL、SSPL 或来源不明的新增依赖时必须先完成人工兼容性审查。

## 3. v4 产品边界

### 3.1 必须建设

- Markdown 文件与文件夹管理、单一当前文件编辑、快捷捕获、磁贴和自动保存。
- Wiki-Link、Markdown 相对链接、内容嵌入、反向链接和引用跳转。
- 当前文件局部图谱与独立的全局知识图谱。
- 原生知识树：以 `.mindmap.md` 保存严格父子层级，用于数学、哲学等复杂学科的总览树、分支子树和 Markdown 概念笔记网络。
- 原生 JSON Canvas 1.0 知识画布，支持 `text`、`file`、`link`、`group` 节点和标准边。
- 全文搜索、语义搜索、知识库问答、来源引用和 AI 整理建议。
- 可添加、删除、拖拽、缩放和持久化布局的自由卡片仪表盘。
- 文件夹颜色、Light/Dark/Warm 主题和统一 Design Tokens。

### 3.2 禁止重新引入

- 隐藏 `.mindmaps/` 目录、专用脑图 JSON、脑图索引文件和按单篇笔记绑定的旧运行时模型。
- `.xmind`、`.mm` 导入导出、旧脑图专用 `.canvas` 迁移/编辑逻辑和旧格式兼容层。
- 自由定位、任意跨节点连线、网页卡片、装饰节点等与 JSON Canvas 重叠的脑图能力。
- 星系图、星环图、Galaxy 卡片及非引用型装饰可视化。
- 旧思维导图 Store、Hook、Parser、Rust 命令、专用节点边和相关测试。
- 本地 `isPro` 标志、虚假 Pro 付费墙或按层级锁定现有功能。
- Capacitor 移动端适配。
- 重复的图谱组件、重复的统计面板和前端全库正文扫描。

`.mindmap.md` 是正式 Markdown 知识文件，符合 JSON Canvas 1.0 的 `.canvas` 是正式画布文件。二者不得互相转换或共用业务模型。Constellation 尚未发布，不保留旧脑图格式的运行时兼容、导入或转换能力；发现 `.mindmaps/`、`.xmind`、`.mm` 或旧专有脑图文件时只提示不受支持，不得自动删除实际工作区文件。

### 3.3 延后至 Lab

- AI Agent 和未经确认的自动改写、自动移动、自动链接。
- 插件系统、插件市场、WebGPU、移动端和多人协作。
- 时间线视图及其他非核心实验性可视化。

## 4. 全局架构约束（System Context）

Agent 在架构设计、技术选型和代码实现时不得违背以下决策。

### 4.1 窗口与工作区

- 默认使用单主窗口：左侧应用内文件树，右侧为单一当前文件编辑器及图谱、知识树、JSON Canvas 和仪表盘视图；编辑区禁止显示 Tab 栏。
- 文件树当前选中的文件是唯一上下文。局部图谱、知识树和 JSON Canvas 视图必须明确显示它们正在围绕哪个当前文件工作；`.mindmap.md` 和 `.canvas` 仍按真实文件类型独立打开。
- 体系化整理使用文件栏中的可选“专题”。“专题”只是通用功能名，每个专题的实际名称由用户自由填写；专题只保存聚焦范围、局部索引和关联视图配置，不复制正文、不替代真实文件夹，也不得成为顶部固定身份选择器。
- 用户不创建专题时仍可完整使用文件树、编辑器、引用、图谱、知识树、JSON Canvas 和仪表盘。数学、哲学、学习、写作、媒体选题等内容均可直接使用真实文件夹组织。
- 专题面板默认折叠；折叠摘要必须保留当前专题颜色、名称和专题数量。展开后才显示完整专题列表，切换或新建专题时摘要必须同步更新。
- 最左侧应用图标栏是编辑器、图谱、知识树、JSON Canvas 和仪表盘的唯一常驻视图入口。文件面板上方不得再放置“文件 / 知识树 / 图谱 / Canvas”等重复视图按钮。
- 文件树只显示真实文件夹、Markdown、`.mindmap.md`、标准 `.canvas` 和附件，并必须支持清晰缩进、展开与折叠。知识图谱是派生视图，禁止在文件树中伪装成文件或目录。
- 文件列表不得提供“全库入口”“收件箱”或全局附件栏目。附件只作为具体 Markdown 笔记引用的工作区相对文件存在，不得设计成邮件式收件工作流。
- “同步与留痕”状态区默认折叠，只显示按需能力摘要；用户展开后才显示具体同步与 Git 状态。未明确启用多人协作时，界面不得使用成员、共享参与者或协作已开启等暗示。
- 快捷便签必须保存为可见 `快捷便签/` 目录中的真实 Markdown。文件栏在最后一个真实文件之后显示可折叠“快捷便签”暂存区；文件区和便签区只用标签与细分隔线区分，复用同一组 Design Tokens，不得增加独立外框或第二套颜色。
- 系统允许多个未整理快捷便签，但同一时间只有一个活动草稿。关闭只隐藏窗口；再次打开默认续写活动草稿。新建不得自动整理旧草稿，用户确认后才通过应用内目录选择器移动到目标真实文件夹。
- 右侧关系栏折叠时，正文必须使用居中的自适应阅读版心并适度加宽；不得继续固定靠左而在右侧留下疑似预留功能区的大块空白。关系栏展开时可缩窄版心，但仍应保持视觉居中。
- 保留 floral 风格的快捷便签、磁贴窗口，以及将当前文件视图分离为独立置顶小窗口的能力。
- 独立窗口只是同一工作区的视图，不得维护另一份笔记正文或索引状态。
- 普通 Markdown 默认使用所见即所得的可视化编辑区，直接编辑渲染后的标题、段落、列表、引用和引用胶囊；Markdown 源码只能通过“更多/高级”操作显式打开，不得在默认界面中裸露源码或长期并排显示源码与预览。
- 主编辑区必须清晰区分“专题上下文、文件标题、正文结构”：专题使用小号眉题，不参与 Markdown 标题层级；顶部文件标题是文档唯一 H1；正文默认从 H2 开始。
- Markdown 源码中的首个 H1 与顶部文件标题表示同一事实，可视正文不得再渲染第二个 H1。额外 H1 必须提示降级或转换，不得制造两个视觉主标题。
- 默认编辑工具栏只保留撤销/重做、正文、H2、H3、粗体/斜体/普通下划线/删除线/行内代码、文字感悟、左/中/右对齐、列表/引用/分割线以及链接/引用/图片/附件等高频操作。H1 仅作为顶部文档标题的只读层级提示；字体、颜色、行距等低频排版进入设置或高级菜单。
- 普通下划线使用可读的 `<u>` 内联 HTML；文字感悟使用选区末尾的 `感₁` 上标和 `360–480px` 横向悬浮卡片。感悟不得嵌套为 Markdown 块引用，正式实现优先使用 Markdown 脚注和 AST 维护锚点。
- 正文存在有效文字选区时显示文字选区浮动工具条，只包含块引用、普通下划线和文字感悟；工具条动作必须复用顶部编辑命令，不得维护第二套正文状态。
- 自定义右键菜单必须由页面、命中对象和当前状态共同决定；不适用的动作不得显示。首层最多六个主要动作，页面空白区域最多四个动作，低频项进入“更多”。
- 图谱菜单只允许浏览、筛选和跳转；知识树“从树中移除”不得删除真实文件；仪表盘“移除卡片”不得删除笔记；AI 建议菜单不得绕过差异预览直接写入。
- 编辑正文获得光标时只能显示文字插入光标和必要的局部选区反馈，禁止绘制贯穿整篇正文的侧边焦点色条。正文与关系面板的滚动条必须使用透明轨道、细圆角中性色滑块并适配 Light/Dark/Warm，禁止暴露白色系统原生滚动条。
- 标题层级等核心工具栏菜单必须使用项目 Design Tokens 控制的圆角弹层和选中态，不得依赖无法稳定适配主题、圆角和密度的系统原生 `<select>` 展开界面。
- 文件树中的文件夹和文件按钮必须清除浏览器原生按钮外观，普通、悬停、选中和折叠状态全部使用 Design Tokens；Light/Dark/Warm 下禁止出现白色系统按钮底色。
- 可视化编辑器与源码模式必须操作同一文档 revision 和 Markdown 事实源。正式实现必须使用结构化编辑器模型与 Markdown AST 双向转换，禁止使用 `innerHTML`、正则替换或两份独立正文状态完成持久化。

### 4.2 纯文件系统存储

- Markdown 正文、`.mindmap.md` 知识树、标准 `.canvas` 文件、附件和真实文件夹是唯一事实源，禁止使用数据库存储笔记正文、知识树正文或画布正文。
- Rust 后端通过 `notify` 监听外部变更。
- `.constellation/` 只保存配置、仪表盘布局、建议状态、迁移状态和可重建索引。
- `.constellation/cache/` 不参与同步；删除全部索引后必须能够从工作区完整重建。
- 附件统一使用工作区相对路径。

### 4.3 Rust Core Engine 所有权

Rust Core Engine 负责工作区安全、文档与画布写入、引用解析、图谱查询、搜索索引、向量检索、AI 网关、文件监听、界面状态持久化、备份和迁移。

核心服务边界：

- `WorkspaceService`：工作区生命周期、路径约束和权限检查。
- `DocumentService`：文件、文件夹、附件、回收站、原子写入和 revision 检测。
- `ReferenceService`：解析 Wiki-Link、Markdown 链接和内容嵌入。
- `GraphService`：生成局部和全局图谱查询结果。
- `KnowledgeTreeService`（待实施）：结构化解析和写入 `.mindmap.md`、校验严格树结构、执行节点重排/提升/降级、查询子树和学习覆盖率。
- `CanvasService`：校验并原子读写 JSON Canvas 1.0 文件、revision 和工作区相对文件引用。
- `SearchService`：Tantivy 全文索引及中文查询。
- `VectorService`：段落切块、嵌入和语义检索。
- `AiService`：模型适配、流式问答、来源组装和整理建议。
- `DashboardService`：保存工作区仪表盘布局并提供卡片查询入口。
- `WatcherService`：监听外部修改并增量更新派生数据。
- `JobManager`：统一管理索引重建、迁移、备份、同步等长任务的进度、取消和工作区归属。
- `BackupService`：加密备份和恢复。
- `MigrationService`：复制迁移 v3 数据并输出报告。

前端只能通过 Typed Tauri API 调用公开能力，不得加载全库正文后自行建立搜索索引、引用索引或图谱。
旧 `notes_*`、`categories_*` 和 `metadata.json` 运行时模型必须一次性硬切换到 `DocumentService`/`WorkspaceService`，不得长期保留双轨事实源。所有前端 Tauri 调用必须收敛到 `src/core-client/`。

### 4.4 引用与图谱

真实关系仅来自：

- `[[文件]]` Wiki-Link。
- `[文字](../路径/文件.md)` Markdown 相对链接。
- `![[文件#标题]]` 文件或段落嵌入。
- 指向 `constellation_type: source` 的 `.source.md` 标准相对链接；URL 与 AI 外部信息通过该可读 Markdown 证据记录表达，不得引入私有引用语法。

反向链接由真实关系自动推导。AI 相似关系必须与真实引用分开存储、分开查询、分开渲染，并默认关闭。

- 局部图谱以当前文件为中心，默认展示一至二跳。
- 全局图谱用于观察整个知识库的显式引用与知识簇。
- 知识树中的 Markdown/子树链接进入独立的 `knowledgeTree` 结构关系；反向链接可显示“出现于知识树”，知识图谱默认隐藏该关系并提供独立筛选。
- 本地文件夹颜色存于 `.constellation/folders.json`；URL 与 AI 来源集合颜色存于 `.constellation/source-collections.json`。本地引用继承目标文件所在文件夹颜色，URL/AI 引用继承其来源集合颜色；胶囊、竖线、来源预览框、图谱节点和显式来源边必须使用同一解析颜色，禁止保存视图级颜色覆盖。统一调色盘可编辑文件夹或来源集合颜色，默认流程保持为“色环取色 -> 选择原始色或 UI 安全色 -> 应用”；RGB/HSL、明度、饱和度、透明度、安全色强度、对比度和历史管理等低频参数必须放入设置的“高级取色”。
- 引用胶囊的圆形标记必须使用“胶囊色实心圆 + 胶囊背景色挖空内容”直接区分来源类型：本地文件挖空折角文件、URL 挖空窄体 `U`、AI 来源挖空大写 `AI`。三种 SVG 图案独立校准并严格居中：本地文件使用 `11px` 画布和 `1.4px` 线宽，保留圆内留白；URL 使用 `10px` 画布和 `1.5px` 线宽，避免 U 过宽；AI 使用 `11px` 画布和 `1.4px` 线宽，A 与 I 保留间距，I 只能是一条无上下横线的圆头竖线。不得使用字体字形代替 URL/AI 图案，也不得强制三种图案共用尺寸或线宽；本地文件胶囊继续继承目标文件所在彩虹文件夹颜色。
- 引用矩形框是统一的来源片段预览容器，不是文本专用框；内部按文本、图片、视频、音频、PDF、网页或混合内容选择预览器。媒体类型使用图案或徽标表达，不占用来源颜色。
- 外部 URL 与 AI 来源引用必须拥有稳定证据记录。用户确认引用时默认尝试保存最小范围证据截图或媒体缩略图、定位信息、采集时间、内容哈希和验证结果；禁止默认保存完整网页、完整第三方视频、音频或受版权保护媒体。证据记录和用户确认的证据附件属于同步知识资产，不得放入 `.constellation/cache/`。
- AI 只可校验引用目标可访问性、定位是否存在以及用户表述与选中片段的对应程度，不得把截图、AI 输出或 AI 判断声明为事实真实性证明。来源不可访问、定位缺失或验证失败时允许保存，但必须标记为未验证、冲突或来源失效。
- AI 生成内容按生成来源保留 AI 身份，即使已写入本地 Markdown。整个文件引用使用本地身份；明确指向 AI 生成片段的引用使用 AI 来源集合颜色，并通过中性 `storedIn` 关系连接保存它的本地文件。人工修订不得抹除 AI 来源，只增加修订状态。
- 来源失效或内容变化时必须保留原证据记录、来源集合颜色和历史截图，并明确标记为历史采集；重新绑定必须保留旧身份和修复历史，不得静默覆盖。
- 图谱渲染使用 Pixi.js/Canvas 与 Worker 布局；React DOM 只承载工具栏、Tooltip 和面板控件。
- 图谱只负责浏览和跳转，不得演变为知识树或画布编辑器。知识树与 JSON Canvas 都是独立文件类型和工作区，不得复用图谱业务模型或把图谱边写入正文文件。

### 4.5 AI 数据库边界

- 支持 OpenAI-compatible API 与 Anthropic Messages API，不在本阶段实现 Ollama 或其他原生供应商协议。
- OpenAI-compatible 模式在配置可用 embeddings 接口时支持语义/混合检索；Anthropic 模式禁用语义检索，只使用 Tantivy 全文来源进行有依据问答。
- 首次外发正文前必须明确展示服务商、数据范围和允许读取的文件夹。
- OpenAI 与 Anthropic API Key 必须按供应商分别写入系统安全凭据存储，禁止写入工作区、日志或前端持久化状态。
- 检索按段落切块，混合关键词、语义、标题和链接权重。
- AI 回答必须包含来源文件、引用段落和可跳转位置；依据不足时必须明确说明。
- AI 引用校验结果只能为直接支持、部分支持、仅相关、冲突、定位缺失、不可访问或未检查；来源不可访问或定位不存在时禁止返回“已验证”。
- 知识树层级只可用于查询扩展、导航和结果排序，不得把节点标题或父子关系当作回答证据；回答引用仍必须落到真实 Markdown 段落。
- AI 生成的学科分支只能进入带来源和差异预览的建议流程，未经确认不得改写 `.mindmap.md`。
- AI 摘要、标签、分类、移动和链接结果先进入“建议收件箱”。
- 未经用户逐项或批量确认，AI 不得改写 frontmatter、正文、文件路径或引用。
- 禁用 AI 后，记录、编辑、搜索、引用和图谱仍必须完整可用。

### 4.6 知识树

- 知识树用于把数学、哲学等大型学科拆成“总览树 -> 分支子树 -> Markdown 概念笔记”的分层地图网络，不用于自由脑暴画布。
- 学科总览 `.mindmap.md` 应直接位于对应学科根目录，与分支目录同级；分支子树位于各自分支目录。不得把覆盖整个学科的总览树放入“基础”“方法论”等单一分支目录。
- 文件使用 `名称.mindmap.md` 双后缀，并在 frontmatter 声明 `constellation_type: mindmap`；后缀负责快速路由，frontmatter 负责自描述和一致性校验。
- 正文使用一个 H1 根节点和嵌套 Markdown 列表。结构节点保存普通列表文字，知识节点链接 `.md`，子树入口链接 `.mindmap.md`。
- 结构节点可一键提升为真实 Markdown 笔记；删除或移动知识树节点只修改树结构，不得删除被链接的真实文件。
- 知识树只允许父子层级、同级排序、折叠和自动布局。禁止保存自由坐标、任意连线、网页卡片、分组框和装饰节点。
- 默认提供横向树编辑和放射概览，两者都由布局引擎自动计算；支持聚焦分支、面包屑、搜索、缩略导航、拖拽改变父级和会话内撤销/重做。
- 结构节点继承当前知识树所在文件夹颜色；知识节点和子树入口使用目标文件所在彩虹文件夹颜色；失效链接使用中性错误状态。
- 学习覆盖层由每个 `.mindmap.md` 通过 `learning_overlay: true/false` 独立启用；关闭时知识树作为纯研究整理结构，不显示学习徽标或覆盖统计。全局设置只决定新建知识树的默认值。
- 启用后，轻量学习状态为 `notStarted`、`learning`、`review`、`mastered`，写入被链接 Markdown 的 frontmatter；纯结构节点只显示是否已形成笔记，不存伪造掌握度。
- Rust 后续负责结构化解析、未知 frontmatter 保留、路径约束、revision 冲突和原子写入；前端不得用脆弱字符串替换修改列表层级。
- 旧隐藏脑图 JSON、`.xmind` 和 `.mm` 不提供运行时兼容或导入；不得恢复旧 Store、Parser 或专用事实源。

### 4.7 JSON Canvas 知识画布

- 文件格式严格遵循 JSON Canvas 1.0，只使用标准 `text`、`file`、`link`、`group` 节点及标准 edges；未知字段读取后应尽量原样保留。
- 文件节点只能引用工作区相对路径；`subpath` 可定位 Markdown 标题或块。所有路径必须经过工作区根目录约束。
- Rust 负责结构校验、ID 唯一性、边端点存在性、revision 冲突和原子保存；撤销/重做历史保留在当前前端会话。
- 网页卡片的 `.canvas` 文件只保存 URL。用户主动请求后获取的标题、站点名、摘要和抓取时间进入 `.constellation/cache/link-metadata.json`，不保存整页正文或第三方受版权保护的页面快照。
- Canvas 的网页元数据缓存与引用证据包必须分离：前者可删除重建；后者是用户确认保存的来源记录与最小范围证据附件，位于可见工作区来源目录并参与同步、备份和回收站。
- Canvas 与图谱、知识树完全分离。禁止自动把引用图谱或知识树改写成画布，也禁止让知识树获得自由定位和任意连线。

### 4.8 自由卡片仪表盘

- 仪表盘是 v4 核心工作区，不是图谱的附属页面。
- 卡片支持添加、删除、拖拽、缩放、尺寸预设和按工作区保存布局。
- 仪表盘网格应使用紧凑自动补位，并通过可组合的卡片尺寸尽量铺满整行；保留必要间距，不得因固定跨列组合产生可被现有卡片填补的大型空洞。
- 可提供“编辑活动日历”摘要卡：月历日期格按修改次数显示热力深浅，并显示涉及文件数；点击日期展示当天文件摘要。未来查询由 Git 提交历史与本地保存事件聚合，前端不得扫描 Git 仓库或全库文件自行统计。本地保存事件属于设备活动状态，不作为笔记事实源；完整逐事件时间线仍属于 Lab。
- 仪表盘布局保存到 `.constellation/dashboard.json`；当前文件会话、快捷便签尺寸、磁贴位置和窗口坐标属于设备状态，保存到 Tauri 应用数据目录，不随工作区同步。
- 卡片只能调用公开查询接口，不得直接扫描或修改文件。
- 卡片数据异步加载并隔离错误；单卡失败不得导致整个仪表盘白屏。
- 视觉采用 FlClash 式模块秩序与 floral 的柔和颜色、圆角和轻量阴影。

### 4.9 同步、冲突与加密

- Core 优先兼容 OneDrive、iCloud、Dropbox、Syncthing 等文件夹同步工具。
- 工作区默认按个人本地知识库表达，也可由用户主动指向共享文件夹；共享文件夹兼容只表示复用现有文件同步工具，不等同于应用内成员、角色、权限、审批或实时协作系统。
- 需要明晰修改权责的共享文件夹应启用 Git 留痕，并展示作者、时间、文件和变更量摘要。未启用 Git 时仍可同步，但界面必须明确标记“留痕不完整”，不得把文件修改时间冒充完整作者历史。
- Git 与同步状态属于工作区状态面板，不属于文件树或专题列表。正式成员管理、权限控制、在线协作和服务端审计仍延后至 Lab。
- 文档写入必须使用临时文件加原子替换，并结合内容哈希与 revision 检测。
- 冲突时保留双方文件，禁止静默覆盖，并在仪表盘显示冲突状态。
- 工作区默认保持明文；加密只覆盖备份包和未来远端传输。
- 加密使用 Argon2id 与 XChaCha20-Poly1305。
- 内置 WebDAV 与 Git 快照属于后续高级能力，不得阻塞 Core。

### 4.10 主题与渲染

- DOM 层使用 React 19、Tailwind 和 CSS Variables/Design Tokens。
- 官方首批主题为 Light、Dark、Warm 三套 floral 预设。
- 第一阶段允许通过 CSS 主题包扩展视觉，但不得在主题中注入业务逻辑。
- Pixi.js/Canvas 可用于知识图谱、知识树和独立 JSON Canvas 工作区的高密度渲染，但知识树必须保持严格树结构，禁止非引用型装饰可视化。

## 5. 功能分层

功能层级表示成熟度和复杂度，不表示付费权限。

| 层级 | 功能范围 |
|------|----------|
| **Core** | Markdown 文件管理、当前文件编辑、快捷捕获、磁贴、自动保存、知识树手动编辑、JSON Canvas 1.0、文件夹颜色、引用与嵌入、反向链接、局部图谱、全文搜索、自由卡片仪表盘 |
| **Plus** | 全局图谱、AI 语义搜索、知识库问答、知识树来源化分支建议与结构诊断、来源引用、标签/摘要/分类建议、相关碎片推荐、加密备份 |
| **Pro** | 高级混合检索、批量 AI 整理、WebDAV、Git 快照、多工作区、知识库诊断和大型库优化 |
| **Lab** | AI Agent、自动链接、时间线、社区插件、WebGPU、移动端和协作 |

任何层级都不得通过本地布尔值伪造付费墙。若未来引入商业策略，必须另行设计可靠的授权和开源合规方案。

## 6. 数据模型约束

Markdown 使用稳定身份：

```yaml
---
constellation_id: "uuid-v7"
created: "2026-06-07T12:00:00Z"
tags:
  - 灵感
---
```

知识树也是 Markdown，并额外使用：

```yaml
---
constellation_id: "uuid-v7"
constellation_type: mindmap
created: "2026-06-14T00:00:00Z"
---
```

规则：

1. 标题优先取首个 H1，否则取文件名。
2. 文件路径和真实文件夹是分类来源。
3. 本地文件夹颜色存储在 `.constellation/folders.json`，作为本地文件树、引用、知识树和图谱节点的颜色事实源；URL/AI 来源集合颜色存储在 `.constellation/source-collections.json`，作为外部来源胶囊、预览框、图谱节点和显式来源边的颜色事实源。各视图不得保存独立覆盖值。
4. `constellation_id` 在移动和重命名后保持不变。
5. 读写 frontmatter 时必须保留用户未知字段。
6. AI 摘要和未确认建议默认只存于派生索引或建议状态中。
7. 新建文档必须生成 UUID v7；旧文档通过迁移或首次受控写入补齐。
8. 文件身份、路径、标题、内容哈希和 revision 必须分开建模，不得混用路径作为永久主键。
9. `.mindmap.md` 必须同时满足双后缀和 `constellation_type: mindmap`；不一致时提示修复或显式转换，不得静默猜测。
10. 知识树正文只保存 H1、嵌套列表和标准 Markdown 链接；布局、折叠和选中状态属于可重建或设备会话状态。
11. `learning_overlay` 属于单个知识树的可选 frontmatter 配置；不得使用全局开关覆盖已有知识树的选择。
12. 外部来源证据使用可见工作区中的 `.source.md` 与相对附件路径；正文引用必须使用标准 Markdown 相对链接。裸 URL 只有在用户提升为来源证据后才获得稳定证据身份、验证状态和图谱来源节点。

## 7. 核心体验红线

- **绝对禁止**在核心交互中调用系统原生文件管理器、打开/保存弹窗或资源管理器。
- 文件和文件夹的创建、重命名、移动、删除、搜索必须在应用内完成。
- 必须支持拖拽反馈、幽灵态、放置目标提示和失败回滚。
- 必须支持行内重命名。
- 必须采用 Optimistic UI；后端写入失败时恢复前端状态并给出可操作错误。
- 保存、索引和 AI 请求不得阻塞编辑器输入。
- 记录一条碎片到可搜索状态最多只需要一次保存操作。
- 删除、移动和批量 AI 写入属于高风险操作，必须可撤销或进入回收站。

## 8. 迁移与兼容规则

1. v3 工作区必须先备份再复制迁移，禁止直接修改原工作区。
2. 迁移 Markdown 时补齐稳定 UUID，并保留未知 frontmatter 字段。
3. 建立旧 ID 到新 UUID/路径的映射。
4. `.mindmap.md` 作为普通 Markdown 安全复制并保留原路径；旧隐藏脑图 JSON、`.xmind` 和 `.mm` 不迁移、不转换、不自动删除，只在报告中标为不受支持。
5. 结构化识别 `.canvas`：符合 JSON Canvas 1.0 的文件原路径保留；无法确认格式的遗留 Canvas 不得当作知识树打开。
6. 输出机器可读和用户可读的迁移报告，包括成功项、冲突项和未迁移项。
7. 迁移重复执行时应尽量幂等，不得生成无限重复文件。
8. 任何无法自动判断的冲突都必须保留双方数据并交由用户处理。

## 9. 目录与模块边界

目标目录：

```text
src/
├─ app/
├─ core-client/
├─ workspace/
├─ editor/
├─ capture/
├─ canvas/
├─ references/
├─ graph/
├─ knowledge-tree/
├─ search/
├─ ai/
├─ dashboard/
├─ settings/
├─ design-system/
└─ shared/

src-tauri/src/
├─ api/
├─ engine/
├─ workspace/
├─ documents/
├─ references/
├─ graph/
├─ knowledge_tree/
├─ search/
├─ vector/
├─ ai/
├─ watcher/
├─ dashboard/
├─ jobs/
├─ backup/
├─ migration/
└─ platform/
```

- 新代码优先进入对应领域目录，禁止继续扩大无边界的 `components/`、`utils/` 或单体 Store。
- 跨领域调用通过公开接口完成，UI 组件不得绕过 `core-client` 直接拼装 Tauri 命令。
- 共享模块只放稳定、无领域归属的基础能力，不能成为循环依赖中转站。
- 删除旧模块时同时清理入口、路由、Store、类型、IPC 命令、依赖、测试和 README 描述。

## 10. 开发命令与验证

| 命令 | 说明 |
|------|------|
| `npm run tauri dev` | 启动桌面端开发（Tauri + Vite） |
| `npm run tauri build` | 构建发布版本并验证打包资源 |
| `npm run build` | 执行前端类型检查与生产构建 |
| `npm run test` | 运行 Vitest 测试 |
| `npm run dev` | 仅启动 Vite 前端 |
| `cargo test --manifest-path src-tauri/Cargo.toml` | 运行 Rust 测试 |
| `cargo fmt --manifest-path src-tauri/Cargo.toml --check` | 检查 Rust 格式 |

验证范围必须与改动风险匹配：

- 文档改动至少检查 Markdown、链接和版权章节。
- 前端行为改动运行相关测试及 `npm run build`。
- Rust Core 改动运行相关 Rust 测试；涉及 IPC 时同时验证前端类型。
- 文件写入、迁移、冲突、加密和删除逻辑必须包含失败路径测试。
- 图谱和搜索改动必须验证增量更新以及删除索引后的完整重建。
- Canvas 改动必须覆盖标准节点/边往返、未知字段保留、路径穿越、损坏 JSON、悬空边和 revision 冲突。
- 知识树改动必须覆盖 Markdown AST 往返、三类节点、拖拽改父级、子树链接、失效链接、学习状态、结构关系、路径穿越和 revision 冲突。
- 发布前检查根 `LICENSE` 未被改写、构建产物包含许可证，并生成第三方依赖许可证清单。

## 11. README 同步更新规则

每次完成新增、修改或删除功能后，Agent 必须在提交前同步更新 `README.md`：

1. **功能特性**：使用 `- **功能名** — 描述` 格式。
2. **模块完成度**：使用 `| 模块 | 完成度 | 说明 |` 表格。
3. **技术架构**：技术栈、服务边界或数据流发生变化时更新。
4. **项目结构**：新增、移动或删除重要目录时更新。
5. **删除与延后**：被砍掉或转入 Lab 的能力必须明确标记，禁止继续展示旧完成度。
6. **版权与致谢**：始终保留 floral-notepaper、Achilng、Maplar 和 MIT 许可证说明。

推荐操作流程：

1. 阅读现有实现和相关测试。
2. 完成最小范围的代码修改。
3. 运行与风险匹配的测试和构建。
4. 更新 `README.md` 和必要的架构文档。
5. 检查 `LICENSE`、版权头、构建资源和 Git diff。
6. 生成符合 Conventional Commits 的提交信息，例如 `feat(references): add incremental backlink index`。
7. 仅在用户确认后执行提交和推送。

## 12. Agent 实施原则

- 不得回滚或覆盖用户未要求处理的现有改动。
- 优先复用项目已有模式；新增抽象必须解决明确的复杂度或重复问题。
- 文件、frontmatter、Markdown 和 JSON 必须使用结构化解析器，禁止依赖脆弱的字符串替换。
- 所有路径必须经过工作区根目录约束与规范化，禁止目录穿越。
- 所有正文写入必须可检测冲突并尽量原子化。
- 索引、缓存和 AI 建议必须可删除、可重建、可追踪来源。
- AI 输出是不可信输入，展示和写回前必须校验。
- 10,000 文件规模下，前端不得加载全库正文；索引和图谱必须增量更新。

## 13. 已确认的后端实施顺序（方案已锁定，尚待实施）

1. **规范与契约**：先更新本文档、README 和 Typed IPC 类型；统一错误为 `{ code, message, details, retryable }`，统一长任务为可取消 `jobId`。
2. **Core 硬切换**：一次性将主编辑器、快捷便签、磁贴和文件树从旧 `notes_*`/`categories_*` 迁移到 `WorkspaceService`/`DocumentService`，随后删除旧命令和 `metadata.json` 运行依赖。
3. **引用与检索闭环**：让应用内保存和外部 watcher 共用同一增量管线，更新引用、反向链接、局部/全局图谱和 Tantivy 索引。
4. **知识树**：实现 `.mindmap.md` 契约、结构化读写、三类节点、子树导航、学习覆盖层和独立结构关系。
5. **Canvas 与仪表盘**：实现 JSON Canvas 1.0 读写、网页元数据缓存、仪表盘后端持久化和设备状态分离。
6. **AI 供应商适配**：接入 OpenAI-compatible 与 Anthropic；流事件固定为 `sources -> delta -> coverage -> done`，支持取消和来源跳转；知识树 AI 建图只进入有来源的建议流程。
7. **高级服务闭环**：统一建议、诊断、WebDAV、Git 快照、加密备份、迁移和后台任务生命周期。
8. **全量验收**：所有已确认 UI 中每个可点击操作必须执行真实动作或显示可恢复的配置/离线错误；禁止假数据、假按钮和静默失败。
9. **发布合规**：复核根 `LICENSE`、源码版权头、安装包许可证资源、清单许可证字段和第三方依赖许可证报告。

## 14. 违规处理

如果 Agent 可能违反版权规则、数据安全要求或 v4 核心架构约束，必须立即停止并输出：

`⚠️ 警告：当前操作可能侵犯原始版权（MIT 协议要求保留声明）或违背 Constellation v4 核心架构约束。操作已中止。`
